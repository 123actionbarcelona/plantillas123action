# Puerto del servidor (por defecto: 3000)
PORT=3333

# Configuración de base de datos
DB_PATH=./templates.db

# Configuración de CORS (para desarrollo)
CORS_ORIGIN=*

# Límite de tamaño para JSON (por defecto: 10mb)
JSON_LIMIT=10mb